# Brand assets

Place the real logo here as:

- `logo.webp`

Use a tightly cropped WebP with the logo centered on a dark background.

If the logo file is missing, the site falls back to the text logo `ДОМ`.
